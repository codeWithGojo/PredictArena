import { PrivatePlaceholder } from "@/components/auth/private-placeholder";
export default function SlipsPage() { return <PrivatePlaceholder title="Saved slips" body="Save and revisit your own outcome selections once the secure account API is connected." />; }
